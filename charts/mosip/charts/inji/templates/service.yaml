apiVersion: v1
kind: Service
metadata:
  name: {{ include "inji.fullname" . }}
  labels:
    {{- include "inji.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inji.selectorLabels" . | nindent 4 }}
