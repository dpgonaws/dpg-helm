apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inji.fullname" . }}-test-connection"
  labels:
    {{- include "inji.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inji.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
