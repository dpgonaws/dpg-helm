apiVersion: v1
kind: Service
metadata:
  name: {{ include "esignet.fullname" . }}
  labels:
    {{- include "esignet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esignet.selectorLabels" . | nindent 4 }}
