apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "init-chart.fullname" . }}-test-connection"
  labels:
    {{- include "init-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "init-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
