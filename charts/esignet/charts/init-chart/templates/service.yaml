apiVersion: v1
kind: Service
metadata:
  name: {{ include "init-chart.fullname" . }}
  labels:
    {{- include "init-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "init-chart.selectorLabels" . | nindent 4 }}
