apiVersion: v1
kind: Service
metadata:
  name: {{ include "esignet-charts.fullname" . }}
  labels:
    {{- include "esignet-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esignet-charts.selectorLabels" . | nindent 4 }}
