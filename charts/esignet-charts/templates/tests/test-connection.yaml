apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esignet-charts.fullname" . }}-test-connection"
  labels:
    {{- include "esignet-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esignet-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
