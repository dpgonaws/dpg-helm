apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-init.fullname" . }}
  labels:
    {{- include "postgres-init.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postgres-init.selectorLabels" . | nindent 4 }}
