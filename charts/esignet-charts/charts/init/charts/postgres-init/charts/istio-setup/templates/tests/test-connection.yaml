apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "istio-setup.fullname" . }}-test-connection"
  labels:
    {{- include "istio-setup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "istio-setup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
