apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-setup.fullname" . }}
  labels:
    {{- include "istio-setup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istio-setup.selectorLabels" . | nindent 4 }}
