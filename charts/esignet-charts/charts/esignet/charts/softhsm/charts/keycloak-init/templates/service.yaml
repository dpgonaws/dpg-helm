apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloak-init.fullname" . }}
  labels:
    {{- include "keycloak-init.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keycloak-init.selectorLabels" . | nindent 4 }}
