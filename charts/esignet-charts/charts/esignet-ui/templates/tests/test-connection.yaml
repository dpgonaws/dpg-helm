apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esignet-ui.fullname" . }}-test-connection"
  labels:
    {{- include "esignet-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esignet-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
